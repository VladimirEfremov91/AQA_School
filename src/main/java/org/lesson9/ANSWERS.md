1. Нужно ли было впринципе писать свой собственный генератор с фамилиями? Было бы проще использовать генератор из datafaker?
- В своем собственном генераторе нет смысла, так как использование сторонней библиотеки делает код более читаемым и коротким.

2. Какая из двух библиотек из этого задания может больше пригодится при написании автотестов - jfiglet или datafaker?
- datafaker, потому что генерация тестовых данных - это один из столпов в основании всего процесса тестирования


3. Каким образом JAR-файлы этих библиотек попали к нам в проект?
- Это делает сборщик Graddle, так как по умолчанию этих библиотек нет в стандартной Java.